name: agents_app
description: Demonstrates the agents_flutter chat UI.
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  a2a: ^4.2.0
  agents:
    path: ../agents
  agents_flutter:
    path: ../agents_flutter
  agents_llama:
    path: ../agents_llama
  animations: ^2.2.0
  anthropic_sdk_dart: ^5.0.0
  characters: ^1.4.1
  cross_file: ^0.3.5+2
  cupertino_icons: ^1.0.8
  extensions: ^0.4.1
  extensions_flutter: ^0.4.5
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_context_menu: ^0.4.2
  flutter_markdown_plus: ^1.0.7
  flutter_picture_taker: ^0.2.0
  flutter_test:
    sdk: flutter
  go_router: ^17.3.0
  google_fonts: ^8.1.0
  http: ^1.6.0
  image_picker: ^1.2.2
  mime: ^2.0.0
  path_provider: ^2.1.6
  pool: ^1.5.2
  qr_flutter: ^4.1.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  universal_platform: ^1.1.0
  url_launcher: ^6.3.2
  waveform_recorder: ^1.8.0
  web: ^1.1.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
